apiVersion: v1
kind: ConfigMap
metadata:
  name: curiopay-api-config
data:
  # Application settings
  PORT: "3000"
  NODE_ENV: "production"
  
  # API configuration
  API_PREFIX: "/api"
  SWAGGER_PATH: "/docs"
  
  # Logging configuration
  LOG_LEVEL: "info" 